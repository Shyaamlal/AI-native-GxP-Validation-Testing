# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp
~$*

# Editor files
.vscode/
.idea/
.obsidian/
*.swp
*.swo
*~

# Playwright MCP session captures (per Feature Scoping §6 — session-scoped, not committed)
.playwright-mcp/

# Local Claude Code settings (per-user, not shared)
.claude/settings.local.json

# Python cache (if using Python scripts)
__pycache__/
*.pyc

# Node modules (if using JavaScript)
node_modules/

# Personal notes (if you want to keep private)
PERSONAL_NOTES.md